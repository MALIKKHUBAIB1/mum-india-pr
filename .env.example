# SMTP config for inquiry emails (Gmail app password)
# SMTP_USER is the Gmail that SENDS (needs App Password), SMTP_TO is where enquiries ARRIVE
# Set these in Vercel/Netlify dashboard → Environment Variables
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=Orgofarm15@gmail.com
SMTP_PASS=bszl udmo slay njki
SMTP_TO=Info@mumindiapr.com
