# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Env (contains SMTP secrets - never commit)
.env
.env.*
src/.env
!.env.example

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Build artifacts (regenerated on every build/deploy)
.netlify/
.vercel/

# Local admin CMS data (dev only — do NOT deploy).
# Production seeds fresh at runtime so bundled image URLs resolve correctly;
# use Supabase env vars for permanent production storage.
content.db.json
admin-auth.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
